apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: demo-app-vars
  namespace: demo-app
spec:
  refreshInterval: 30s
  secretStoreRef:
    kind: ClusterSecretStore
    name: doppler-auth
  target:
    name: demo-app-vars
    deletionPolicy: Merge
  dataFrom:
    - find:
        name:
          regexp: .*
