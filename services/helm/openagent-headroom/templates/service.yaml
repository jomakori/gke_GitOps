apiVersion: v1
kind: Service
metadata:
  name: {{ include "openagent-headroom.fullname" . }}
  labels:
    {{- include "openagent-headroom.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openagent-headroom.labels" . | nindent 4 }}
