apiVersion: kagent.dev/v1alpha2
kind: SandboxAgent
metadata:
  name: momus-fb
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kagent.labels" . | nindent 4 }}
    agent-type: fallback
    agent-role: reviewer
spec:
  type: Declarative
  description: "Fallback plan reviewer. Serves as backup when momus primary model fails."
  declarative:
    runtime: go
    modelConfig: opencode-big-pickle
    systemMessageFrom:
      name: kagent-prompts
      key: momus-fb-system.txt
      type: ConfigMap
  platform: substrate
  substrate:
    workerPoolRef:
      name: kagent-default
