apiVersion: kagent.dev/v1alpha2
kind: SandboxAgent
metadata:
  name: librarian
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: kagent
    agent-type: primary
    agent-role: researcher
spec:
  type: Declarative
  description: "Docs/RAG searcher. Find authoritative docs, OSS examples, remote repos."
  declarative:
    runtime: go
    modelConfig: opencode-deepseek-v4-flash-free
    systemMessageFrom:
      configMapKeyRef:
        name: kagent-prompts
        key: librarian-system.txt
  platform: substrate
  substrate:
    workerPoolRef:
      name: kagent-default
