apiVersion: kagent.dev/v1alpha2
kind: SandboxAgent
metadata:
  name: ultraworker-fb
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: kagent
    agent-type: fallback
    agent-role: worker
spec:
  type: Declarative
  description: "Fallback sub-worker. Same role as ultraworker, different model."
  declarative:
    runtime: go
    modelConfig: moonshotai-kimi-k2-6
    systemMessageFrom:
      configMapKeyRef:
        name: kagent-prompts
        key: ultraworker-fb-system.txt
  platform: substrate
  substrate:
    workerPoolRef:
      name: kagent-default
