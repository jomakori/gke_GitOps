apiVersion: v1
kind: Service
metadata:
  name: ramalama
  labels:
    app.kubernetes.io/name: ramalama
spec:
  type: ClusterIP
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app.kubernetes.io/name: ramalama
