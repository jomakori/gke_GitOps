apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "kagent-headroom.fullname" . }}-data
  labels:
    {{- include "kagent-headroom.labels" . | nindent 4 }}
spec:
  accessModes:
    - {{ .Values.storage.accessMode }}
  resources:
    requests:
      storage: {{ .Values.storage.size }}
  {{- with .Values.storage.storageClass }}
  storageClassName: {{ . }}
  {{- end }}
