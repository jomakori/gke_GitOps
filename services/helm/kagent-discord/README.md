# kagent-discord

![Version: 0.1.0](https://img.shields.io/badge/Version-0.1.0-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: 0.1.0](https://img.shields.io/badge/AppVersion-0.1.0-informational?style=flat-square)

Kagent Discord bot — polls Discord and routes messages to A2A agent

## Maintainers

| Name | Email | Url |
| ---- | ------ | --- |
| local |  |  |

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| config | object | `{"channelOnly":"","clientId":"","conversationMode":"threaded","kagentA2aUrl":"http://kagent-controller.kagent.svc.cluster.local:8083/api/a2a-sandboxes/kagent/sisyphus","mentionOnly":true,"phaseUpdates":true,"pollUI":true,"startupChannel":"chat"}` | Discord bot configuration |
| config.channelOnly | string | `""` | Restrict to a specific Discord channel (empty = all channels) |
| config.clientId | string | `""` | Discord application client ID (public, used for invite URL diagnostics) |
| config.conversationMode | string | `"threaded"` | Conversation mode: "threaded" (guild threads) or "dm" (direct messages)    "threaded" = new thread per conversation in guild channels    "dm" = replies in DM channel, thread-like isolation via user ID |
| config.kagentA2aUrl | string | `"http://kagent-controller.kagent.svc.cluster.local:8083/api/a2a-sandboxes/kagent/sisyphus"` | A2A endpoint for the backing agent. In-cluster only.    The bot runs inside the kagent namespace and connects directly    to the controller service. No CF Access, no public domain. |
| config.mentionOnly | bool | `true` | Only respond to messages mentioning the bot |
| config.phaseUpdates | bool | `true` | Phase transition updates in thread/DM    When true, bot posts status updates at phase transitions    (classifying → planning → executing → verifying → done) |
| config.pollUI | bool | `true` | Poll-based UI for direct questions    When true, ambiguous questions render as Discord polls/buttons |
| config.startupChannel | string | `"chat"` | Discord channel ID to send startup/online announcement    Leave empty to skip the announcement |
| dopplerConfig | string | `""` | Doppler config; set by ArgoCD appset. Creates ExternalSecret when present.    Expected keys: DISCORD_BOT_TOKEN |
| image | object | `{"pullPolicy":"IfNotPresent","repository":"ghcr.io/jomakori/gke_gitops/kagent-discord-bot","tag":"0.1.8"}` | Container image |
| resources | object | `{"limits":{"cpu":"500m","memory":"512Mi"},"requests":{"cpu":"50m","memory":"128Mi"}}` | Resource requests/limits |
| service | object | `{"port":8080}` | Service settings (ClusterIP for DNS discovery only — bot has no inbound port) |

----------------------------------------------
Autogenerated from chart metadata using [helm-docs v1.14.2](https://github.com/norwoodj/helm-docs/releases/v1.14.2)
