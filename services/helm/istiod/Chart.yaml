apiVersion: v2
name: istiod
description: Istio control plane — ambient mode with STRICT mTLS mesh-wide
type: application
version: 1.25.2
appVersion: "1.25.2"

dependencies:
  - name: istiod
    version: 1.25.2
    repository: https://istio-release.storage.googleapis.com/charts
