apiVersion: v2
name: longhorn
description: Longhorn distributed block storage with RWX support
type: application
version: 0.2.0
appVersion: "v1.12.0"

dependencies:
  - name: longhorn
    version: 1.12.0
    repository: https://charts.longhorn.io
