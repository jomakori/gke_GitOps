apiVersion: v2
name: opencode-server
description: >-
  OpenCode headless server (the oh-my-opencode agent harness) as its own pod, so
  agent memory never lives in the Hermes gateway container.
type: application
version: 0.1.0
appVersion: "1.18.29"
