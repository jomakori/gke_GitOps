const PORT=parseInt(process.env.PORT??"4524");const UP=process.env.UPSTREAM??"http://127.0.0.1:4523";function srv(){let s;Bun.serve({port:PORT,hostname:"0.0.0.0",websocket:{open(ws){},message(ws,msg){setTimeout(async()=>{try{const b=JSON.parse(msg);const inp=typeof b.input==="string"?b.input:JSON.stringify(b.input);const cr={model:b.model||"claude-sonnet-5",messages:[...(b.instructions?[{role:"system",content:b.instructions}]:[]),{role:"user",content:inp}],stream:true};const cr2=await fetch(UP+"/v1/chat/completions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(cr)});if(!cr2.ok){ws.close(1011,await cr2.text());return}const reader=cr2.body.getReader();const dec=new TextDecoder();let fullText="",buf="";const id="resp_"+crypto.randomUUID().slice(0,24);const mi=id+"_msg";let sentCreated=false;const send=x=>{try{ws.send(x+"\n\n")}catch{}};while(true){const{done,value}=await reader.read();if(done)break;buf+=dec.decode(value,{stream:true});const lines=buf.split("\n");buf=lines.pop()||"";for(const line of lines){if(!line.startsWith("data: "))continue;const s=line.slice(6);if(s==="[DONE]")continue;try{const ck=JSON.parse(s);const d=ck?.choices?.[0]?.delta?.content;if(typeof d!=="string")continue;if(!sentCreated){send("data: "+JSON.stringify({type:"response.created",response:{id,object:"response",status:"in_progress",model:cr.model,output:[],usage:null}}));send("data: "+JSON.stringify({type:"response.in_progress",response:{id,object:"response",status:"in_progress",model:cr.model,output:[]}}));send("data: "+JSON.stringify({type:"response.output_item.added",output_index:0,item:{id:mi,type:"message",status:"in_progress",role:"assistant",content:[]}}));send("data: "+JSON.stringify({type:"response.content_part.added",item_id:mi,output_index:0,content_index:0,part:{type:"output_text",text:""}}));sentCreated=true}fullText+=d;send("data: "+JSON.stringify({type:"response.output_text.delta",item_id:mi,output_index:0,content_index:0,delta:d}))}catch{}}}if(sentCreated){send("data: "+JSON.stringify({type:"response.completed",response:{id,object:"response",status:"completed",model:cr.model,output:[{id:mi,type:"message",status:"completed",role:"assistant",content:[{type:"output_text",text:fullText}]}],usage:{input_tokens:0,output_tokens:0,total_tokens:0}}}))}ws.close(1000)}catch(e){try{ws.close(1011,String(e))}catch{}}},0)},close(ws){}},fetch(req,server){s=server;const u=new URL(req.url);if(u.pathname==="/v1/responses"){if(req.headers.get("upgrade")?.toLowerCase()==="websocket"){const ok=server.upgrade(req);if(!ok)return new Response("WS upgrade failed",{status:400});return}return handleResponses(req).catch(e=>new Response("proxy error: "+String(e),{status:502}))}return fetch(UP+u.pathname+u.search,{method:req.method,headers:req.headers,body:req.body})}})}async function handleResponses(req){const body=await req.json();const inp=typeof body.input==="string"?body.input:JSON.stringify(body.input);const cr={model:body.model||"claude-sonnet-5",messages:[...(body.instructions?[{role:"system",content:body.instructions}]:[]),{role:"user",content:inp}],stream:true};const cr2=await fetch(UP+"/v1/chat/completions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(cr)});if(!cr2.ok){const e=await cr2.text();return new Response(e,{status:cr2.status})}const enc=new TextEncoder();const id="resp_"+crypto.randomUUID().slice(0,24);const mi=id+"_msg";const reader=cr2.body.getReader();const dec=new TextDecoder();let buf="",fullText="",sentCreated=false;const stream=new ReadableStream({async pull(ctrl){const{done,value}=await reader.read();if(done){if(sentCreated){ctrl.enqueue(enc.encode("data: "+JSON.stringify({type:"response.completed",response:{id,object:"response",status:"completed",model:cr.model,output:[{id:mi,type:"message",status:"completed",role:"assistant",content:[{type:"output_text",text:fullText}]}],usage:{input_tokens:0,output_tokens:0,total_tokens:0}}})+"\n\n"))}ctrl.close();return}buf+=dec.decode(value,{stream:true});const lines=buf.split("\n");buf=lines.pop()||"";for(const line of lines){if(!line.startsWith("data: "))continue;const s=line.slice(6);if(s==="[DONE]"){ctrl.enqueue(enc.encode("data: "+JSON.stringify({type:"response.completed",response:{id,object:"response",status:"completed",model:cr.model,output:[{id:mi,type:"message",status:"completed",role:"assistant",content:[{type:"output_text",text:fullText}]}],usage:{input_tokens:0,output_tokens:0,total_tokens:0}}})+"\n\n"));continue}try{const ck=JSON.parse(s);const d=ck?.choices?.[0]?.delta?.content;if(typeof d!=="string")continue;if(!sentCreated){ctrl.enqueue(enc.encode("data: "+JSON.stringify({type:"response.created",response:{id,object:"response",status:"in_progress",model:cr.model,output:[],usage:null}})+"\n\n"));ctrl.enqueue(enc.encode("data: "+JSON.stringify({type:"response.in_progress",response:{id,object:"response",status:"in_progress",model:cr.model,output:[]}})+"\n\n"));ctrl.enqueue(enc.encode("data: "+JSON.stringify({type:"response.output_item.added",output_index:0,item:{id:mi,type:"message",status:"in_progress",role:"assistant",content:[]}})+"\n\n"));ctrl.enqueue(enc.encode("data: "+JSON.stringify({type:"response.content_part.added",item_id:mi,output_index:0,content_index:0,part:{type:"output_text",text:""}})+"\n\n"));sentCreated=true}fullText+=d;ctrl.enqueue(enc.encode("data: "+JSON.stringify({type:"response.output_text.delta",item_id:mi,output_index:0,content_index:0,delta:d})+"\n\n"))}catch{}}}});return new Response(stream,{headers:{"Content-Type":"text/event-stream","Cache-Control":"no-cache","x-robots-tag":"noindex"}})}srv();