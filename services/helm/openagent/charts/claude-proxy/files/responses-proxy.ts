const PORT=parseInt(process.env.PORT??"4524");const UP=process.env.UPSTREAM??"http://127.0.0.1:4523";const cache=new Map();Bun.serve({port:PORT,hostname:"0.0.0.0",async fetch(req){const u=new URL(req.url);if(u.pathname!=="/v1/responses"||req.method!=="POST")return fetch(UP+u.pathname+u.search,{method:req.method,headers:req.headers,body:req.body});const raw=await req.text();const key=raw;const cached=cache.get(key);if(cached&&Date.now()-cached.time<30000){const{buf}=cached;const str=new ReadableStream({start(c){c.enqueue(buf);setTimeout(()=>c.close(),10000)}});return new Response(str,{headers:{"Content-Type":"text/event-stream","Cache-Control":"no-cache"}})}const b=JSON.parse(raw);const inp=typeof b.input==="string"?b.input:JSON.stringify(b.input);const cr={model:"claude-sonnet-5",messages:[{role:"system",content:"You are a helpful coding assistant. Reply with ONLY text. No tool calls."},...(b.instructions?[{role:"system",content:b.instructions}]:[]),{role:"user",content:inp}],stream:false};const cr2=await fetch(UP+"/v1/chat/completions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(cr)});if(!cr2.ok){const e=await cr2.text();return new Response(e,{status:cr2.status})}const cd=await cr2.json();const ct=cd.choices?.[0]?.message?.content??"";const id="resp_"+crypto.randomUUID().slice(0,24);const mi=id+"_msg";const usage=cd.usage||{prompt_tokens:0,completion_tokens:0,total_tokens:0};const enc=new TextEncoder();const rj={id,object:"response",model:b.model||cr.model,output:[{id:mi,type:"message",role:"assistant",content:[{type:"output_text",text:ct}]}],usage:{input_tokens:usage.prompt_tokens||0,output_tokens:usage.completion_tokens||0,total_tokens:(usage.prompt_tokens||0)+(usage.completion_tokens||0)}};const buf=Buffer.concat([enc.encode("data: "+JSON.stringify(rj)+"\n\n")]);cache.set(key,{time:Date.now(),buf});const str=new ReadableStream({start(c){c.enqueue(buf);setTimeout(()=>c.close(),10000)}});return new Response(str,{headers:{"Content-Type":"text/event-stream","Cache-Control":"no-cache"}})}});
