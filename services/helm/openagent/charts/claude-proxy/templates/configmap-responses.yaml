{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: claude-proxy-responses
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "claude-proxy.labels" . | nindent 4 }}
    app.kubernetes.io/component: claude-proxy
data:
  responses.ts: |
{{ .Files.Get "files/responses.ts" | indent 4 }}
{{- end }}
