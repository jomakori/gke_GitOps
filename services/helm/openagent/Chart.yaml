apiVersion: v2
name: openagent
description: OpenAgent resources — Ensemble, ExternalSecrets, VPA, SkillPacks (sympozium control plane deployed separately)
type: application
version: 0.1.0
appVersion: "latest"
maintainers:
  - name: local
# sympozium OCI dependency removed — GHCR packages are private-by-default for org repos.
# Control plane is deployed independently in sympozium-system namespace.
dependencies: []
